# Advent of code 2022

In `C#`!

To run

```
cd project
dotnet run -- <day>
```

replace `<day>` with the day you want to run, e.g. day1, day4 &c

To Test

```
cd test
dotnet test
```

to run all tests.